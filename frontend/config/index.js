import defaultConfig from './default';

let config = {...defaultConfig};

// if (HOW TO CONSUME ENV === 'local') {
//     config = {...config, ...require('./local').defaul }
// }

export default config;